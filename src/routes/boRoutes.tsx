interface Route {
  path: string;
  component: React.FC;
}

const boRoutes: Route[] = [
  // { path: "/admin", component: PageAdminMain },
  // { path: "/store", component: PageStoreMain },
];

export default boRoutes;
