import React from "react";
import { SpinnerContainer, Spinner } from "./LoadingSpinner.style";

const LoadingSpinner = () => (
  <SpinnerContainer>
    <Spinner />
  </SpinnerContainer>
);

export default LoadingSpinner;
